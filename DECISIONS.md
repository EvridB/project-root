# 🧠 Ключевые архитектурные решения

## 1. 🚀 **FastAPI + React**
   Современный, быстрый и типобезопасный стек. FastAPI обеспечивает высокую производительность бэкенда и автоматическую документацию, React — гибкий и реактивный интерфейс.

## 2. 🧩 **Слоистая архитектура**
   Разделение на роуты, сервисы и модели обеспечивает тестируемость, переиспользуемость и чёткое разделение ответственности.

## 3. 🔒 **Атомарный UPDATE вместо блокировок**
   Для защиты от гонок используется `UPDATE ... WHERE` с проверкой статуса. Это исключает необходимость в транзакционных блокировках и гарантирует целостность данных при параллельных запросах.

## 4. 🍪 **Аутентификация по cookie (user_id)**
   Упрощённая схема для демонстрации: после входа пользователю устанавливается cookie с его ID. При необходимости легко заменить на JWT или другую стратегию.

## 5. 🐳 **Docker-контейнеризация**
   Всё окружение (бэкенд, фронтенд, БД) упаковано в контейнеры. Запуск одной командой `docker-compose up` гарантирует воспроизводимость и изоляцию.

## 6. 📦 **Alembic для миграций**
   Версионирование схемы базы данных позволяет безопасно эволюционировать структуру таблиц и легко разворачивать проект на новом окружении.

## 7. 🧪 **Автотесты на конкурентный доступ**
   Написаны тесты, проверяющие корректность обработки гонок (например, одновременный захват заявки). Это гарантирует, что механизм защиты работает стабильно.